#include <iostream>
#include <unordered_set>
using namespace std;

int main() {
    vector<int> nums;
    int n;
    for (int i = 0; i<n; i++) {
        cin >> n; 
        nums[i] = n;
    }
    findDuplicate(nums);
}
int findDuplicate(vector<int>& nums) {
        
}